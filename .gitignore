# Python
__pycache__/
*.pyc

# 임시 메시 (GLB로 변환 후 삭제됨)
*.stl

# 빌드 산출물
_site/

# OS
.DS_Store
Thumbs.db

# wrangler local cache (never commit)
mcp/.wrangler/
mcp/node_modules/

# CI on-demand generation toolchain (generate-part.yml)
/fcenv/
/bin/

# CERN 파라메트릭 3D 백필: R2 전용 (§22 Phase 2 — git엔 해시만)
library/*/cern/*/*.step
library/*/cern/*/*.glb
